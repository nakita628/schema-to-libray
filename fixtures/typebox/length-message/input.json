{
  "title": "Code",
  "type": "object",
  "properties": {
    "code": {
      "type": "string",
      "minLength": 6,
      "maxLength": 6,
      "x-length-message": "Code must be exactly 6 characters"
    }
  },
  "required": ["code"]
}
