{
  "name": "schema-to-library",
  "type": "module",
  "private": true,
  "license": "MIT",
  "scripts": {
    "dev": "pnpm -F @apps/generated dev",
    "deps": "rm -rf node_modules && rm -rf pnpm-lock.yaml && pnpm install",
    "all-deps": "pnpm -F @schema-to-library/types deps && pnpm -F @schema-to-library/zod deps && pnpm -F @schema-to-library/helper deps && pnpm -F schema-to-library deps",
    "check": "pnpm biome check --write ./",
    "typecheck": "tsc --noEmit",
    "build": "pnpm -r --workspace-concurrency=Infinity build",
    "test": "vitest run",
    "coverage": "vitest run --coverage",
    "release": "pnpm install && pnpm build && pnpm -F schema-to-library release"
  },
  "devDependencies": {
    "@biomejs/biome": "^2.1.3",
    "@types/node": "^24.1.0",
    "@vitest/coverage-v8": "^3.2.4",
    "vitest": "^3.2.4"
  },
  "dependencies": {
    "schema-to-library": "link:packages/schema-to-zod"
  }
}
