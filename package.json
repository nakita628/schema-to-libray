{
  "name": "schema-to-library",
  "type": "module",
  "private": true,
  "license": "MIT",
  "scripts": {
    "dev": "pnpm -F @apps/generated dev",
    "deps": "rm -rf node_modules && rm -rf pnpm-lock.yaml && pnpm install",
    "check": "pnpm biome check --write ./",
    "typecheck": "tsc --noEmit",
    "build": "pnpm -r build",
    "ci:build": "pnpm --filter @schema-to-library/zod build && pnpm --filter @schema-to-library/cli build && pnpm --filter @schema-to-library/schema-to-library build",
    "test": "vitest run",
    "coverage": "vitest run --coverage",
    "release": "pnpm install && pnpm build && pnpm -F schema-to-library release"
  },
  "devDependencies": {
    "@biomejs/biome": "^2.1.3",
    "@types/node": "^24.1.0",
    "@vitest/coverage-v8": "^3.2.4",
    "vitest": "^3.2.4"
  }
}
