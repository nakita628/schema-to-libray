/**
 * Re-export schema dependency resolution function
 *
 * @see {@link resolveSchemaDependenciesFromSchema} for details
 */
export { resolveSchemaDependenciesFromSchema } from './dep/resolve-schema-dependencies-from-shema.js'

/**
 * Re-export type generation function
 *
 * @see {@link type} for details
 */
export { type } from './dep/type.js'

/**
 * Re-export utility functions
 *
 * @see {@link normalizeTypes} and {@link toPascalCase} for details
 */
export { normalizeTypes, toPascalCase } from './utils/index.js'
