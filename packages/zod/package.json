{
  "name": "@schema-to-library/zod",
  "private": true,
  "type": "module",
  "main": "dist/index.js",
  "scripts": {
    "deps": "rm -rf node_modules && rm -rf pnpm-lock.yaml && pnpm install",
    "build": "pnpm -F @schema-to-library/cli build && pnpm -F @schema-to-library/helper build && rm -rf ./dist/* && tsc"
  },
  "dependencies": {
    "@schema-to-library/cli": "workspace:^",
    "@schema-to-library/helper": "workspace:^"
  }
}
